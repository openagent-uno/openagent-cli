"""Client-side PAKE primitives vendored from the OpenAgent network protocol.

Callers import concrete symbols from :mod:`.pake`; server coordinator service
and storage exports are deliberately absent from the standalone CLI.
"""
